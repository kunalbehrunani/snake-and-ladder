export class Ladder {
  private _bottom: number;
  private _top: number;
  private _name: string;

  constructor(bottom: number, top: number, name: string) {
    this._bottom = bottom;
    this._top = top;
    this._name = name;
    return;
  }
}
